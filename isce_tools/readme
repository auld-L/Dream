可以查看obsidian 2026-07-11产看脚本的使用方法

`view_isce_interferograms.py` 用于批量读取：

```
merged/interferograms/日期1_日期2/
```

目录中的 ISCE 干涉图，并将每个干涉对绘制成一张 PNG 图片，统一保存到指定文件夹。

每张图片包含：

- 左图：干涉图幅度；
    
- 右图：包裹相位或解缠相位。
    

> [!NOTE]  
> 该脚本查看的是逐对干涉图，不是 MintPy 的 `timeseries.h5` 时间序列产品。

## 运行命令

```
python3 view_isce_interferograms.py \
/media/user/data2/isce_test/work/merged/interferograms \
/media/user/data2/isce_test/work/unwrapped_png \
--pattern filt_fine.unw \
--dpi 300 \
--clip-low 5 \
--clip-high 95
```

其中：

```
/media/user/data2/isce_test/work/merged/interferograms
```

是干涉对输入目录。

```
/media/user/data2/isce_test/work/unwrapped_png
```

是PNG图片输出目录。目录不存在时，脚本会自动创建。

## `--pattern` 参数

`--pattern` 用于指定需要绘制的产品，支持：

```
filt_fine.int
fine.int
filt_fine.unw
fine.unw
```

### `filt_fine.int`

滤波后的复数干涉图。

脚本绘制：

- 左图：干涉图幅度，以dB形式显示；
    
- 右图：滤波后的包裹相位。
    

包裹相位范围为：

$$  
-\pi\leq\phi<\pi  
$$

即大约：

$$  
-3.1416\leq\phi<3.1416\ \mathrm{rad}  
$$

### `fine.int`

未滤波的复数干涉图。

脚本绘制：

- 左图：未滤波干涉图幅度；
    
- 右图：未滤波包裹相位。
    

与 `filt_fine.int` 相比，它通常包含更多相位噪声，但保留了更多原始相位信息。

### `filt_fine.unw`

由滤波干涉图得到的解缠结果。

脚本绘制：

- 左图：解缠文件第一波段中的幅度；
    
- 右图：解缠相位，单位为弧度。
    

解缠相位不再限制于 $-\pi$ 到 $\pi$，可能出现：

```
-20、0、15、30 rad
```

等数值。

### `fine.unw`

使用 `--rmfilter` 时可能生成的去滤波影响解缠结果。

它通常保留滤波干涉图确定的整数相位周数，同时重新使用原始 `fine.int` 的包裹相位。

脚本绘制：

- 左图：幅度；
    
- 右图：去除滤波影响后的解缠相位。
    

如果处理过程中没有启用 `--rmfilter`，该文件可能不存在。

## 其他参数

### `--dpi`

设置输出图片的分辨率：

```
--dpi 300
```

数值越大，图片越清晰，同时文件体积也越大。

### `--clip-low`

设置色标显示的低百分位：

```
--clip-low 5
```

表示将5%分位数作为显示下限。

### `--clip-high`

设置色标显示的高百分位：

```
--clip-high 95
```

表示将95%分位数作为显示上限。

使用5%～95%的显示范围可以减小极端异常值对色标的影响。它只改变图片显示效果，不会修改原始数据。

对于包裹相位 `.int`，右图固定显示：

$$  
-\pi\sim\pi  
$$

因此百分位参数主要影响幅度图和解缠相位图。

## 注意事项

每个数据文件旁边必须存在对应的XML文件，例如：

```
filt_fine.int
filt_fine.int.xml
```

或者：

```
filt_fine.unw
filt_fine.unw.xml
```

幅度的dB转换只用于增强显示效果，不代表已经得到经过辐射定标的 $\sigma^0$ 或 $\gamma^0$ 后向散射系数。

该脚本主要用于检查：

- 干涉条纹是否清晰；
    
- 是否存在大面积失相干；
    
- 是否出现异常条带；
    
- 解缠结果是否存在明显跳变；
    
- 某些干涉对是否存在空值或处理异常。
